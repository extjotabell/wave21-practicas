spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/demojpa
    password: root
    username: root
  sql:
    init:
      data-locations:
      - classpath:carga_sql/joya.sql
      mode: always
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true